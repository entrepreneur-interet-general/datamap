(ns datamap.specs
  (:require [clojure.spec.alpha :as s]
            [clojure.spec.gen.alpha :as gen]))

;; (s/def ::my-string string?)
;; (s/def ::pourquoi (s/tuple int? ::my-string))
;; (gen/generate (s/gen ::my-string))
;; (gen/generate (s/gen ::pourquoi))
;; (gen/sample (s/gen ::my-string))
;; (gen/sample (s/gen ::pourquoi))

